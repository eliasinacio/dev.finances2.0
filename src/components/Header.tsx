import './styles.scss'
import logo from '../assets/logo.svg'

function Header() {

  return (
    <header className="Header">
      <img src={logo} alt="" />
    </header>
  )
}

export default Header
